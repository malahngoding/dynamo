version: "3"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dynamo
    restart: always
    volumes:
      - ./:/usr/src/app
    ports:
      - "3000:3000"
