## Feature Live Streaming Technical Design

### Scope

Feature Live Streaming berfungsi sebagai media pembelajaran bagi user atau peserta malah ngoding yang telah melakukan pembayaran.

### System Interaction / Integration

#### < sequence diagram >

Diasumsikan user pada saat register sudah mengirim data uuid ke tabel validation_payment.
![sequence diagram](https://raw.githubusercontent.com/dennyrismanto/Foto/master/flowchart-youtubestreaming2.jpeg 'sequence diagram')

#### < if any contract request & response >

```php
JSON Request {
	URL : {{URL}}
	type : GET
	body :
		{
		“uuid” : required
		“status_pembayaran” : required
		}
}
```

```php
JSON Response{
“messages” : “Berhasil mendapatkan url”
body: {
	“id” : “id”
	“title” : “string title”
	“url_youtube” : “string url”
	“created_at” : “timestamp”
	“update_at” : “timestamp”
	}

}
```

### Technical Implementation

#### < state diagram>

![statediagram](https://raw.githubusercontent.com/dennyrismanto/Foto/master/diagram-flow-youtubestreaming1.jpg.jpeg 'statediagram')

#### < database schema >

```php
Schema::create('youtube', function (Blueprint $table) {
            $table->id()->autoIncrement();
            $table->string('title')->nullable();
            $table->string('url_youtube');
            $table->timestamps();
});
```

```php
Schema::create('validation_payment', function (Blueprint $table) {
            $table->id()->autoIncrement();
			$table->uuid('users_uuid');
            $table->string('status_pembayaran')->default(false);
            $table->timestamps();
});
```
